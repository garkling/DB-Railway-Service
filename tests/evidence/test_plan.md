# HW3 Test Plan and Results

Generated by `pytest_sessionfinish` at end of session (exit status: 0).

Each entry is the test node id, status, and the test's docstring.

### tests/test_forms/test_cancel_ticket.py::test_cancel_ticket_succeeds — **PASSED**
> Cancelling ticket #1 on IC-701 / 2026-04-01 removes exactly one row from
> `ticket`; all other ticket rows remain untouched and the response is 204.

### tests/test_forms/test_cancel_ticket.py::test_cancel_ticket_not_found — **PASSED**
> Cancelling a ticket number that does not exist returns 404 TicketNotFound.

### tests/test_forms/test_issue_ticket.py::test_issue_ticket_succeeds — **PASSED**
> Issuing a ticket for P003 / IC-701 / 2026-04-01 / 2ND / seat 5C inserts one
> new row in the ticket table with the pricing-derived price_paid and the DB
> defaults for booking_status (`confirmed`) and payment_method (`card`).

### tests/test_forms/test_issue_ticket.py::test_issue_ticket_passenger_not_found — **PASSED**
> Issuing a ticket for a non-existent passenger returns 404 PassengerNotFound
> and inserts NO row (transaction rolls back before the INSERT statement).

### tests/test_forms/test_issue_ticket.py::test_issue_ticket_train_not_found — **PASSED**
> Issuing a ticket for a non-existent train returns 404 TrainNotFound
> and inserts NO row.

### tests/test_forms/test_reschedule_train.py::test_reschedule_train_cascades — **PASSED**
> Changing a train's PK fires ON UPDATE CASCADE on coach and ticket; old PK
> becomes empty across all 3 tables and new PK acquires the same row counts.

### tests/test_forms/test_reschedule_train.py::test_reschedule_train_not_found — **PASSED**
> Rescheduling a train PK that does not exist returns 404 TrainNotFound.

### tests/test_forms/test_reschedule_train.py::test_reschedule_train_pk_clash — **PASSED**
> Renaming a train into a PK that already exists in the seed returns 409
> TrainAlreadyExists; the existing row is not overwritten.

### tests/test_reports/test_boarding_pass.py::test_boarding_pass_returns_full_payload — **PASSED**
> Boarding pass for IC-701 / 2026-04-01 / ticket #1 returns the fully-composed
> payload with operator name, passenger full name, class name, origin/destination,
> and the seeded price 675.00 (UZ/1ST/route 1 from operator_class_route_pricing).

### tests/test_reports/test_boarding_pass.py::test_boarding_pass_ticket_not_found — **PASSED**
> Boarding pass for a non-existent ticket number returns 404 TicketNotFound.

### tests/test_reports/test_route_pricing.py::test_route_pricing_unfiltered_returns_all — **PASSED**
> Unfiltered pricing schedule returns all 10 seeded rows; every row has
> operator_name / class_name / route_name populated by the enrichment join.

### tests/test_reports/test_route_pricing.py::test_route_pricing_filtered_by_operator — **PASSED**
> Filtering by operator=UZ returns the 6 UZ pricing rows (1ST/2ND/SLP × routes 1/2);
> every returned row has operator_code == "UZ".

### tests/test_reports/test_route_pricing.py::test_route_pricing_empty_filter_combination — **PASSED**
> A filter combination with zero matching rows (PKP doesn't price route 1)
> returns 200 with rows=[] — NOT a 404. Empty results are valid.

### tests/test_reports/test_train_manifest.py::test_train_manifest_returns_all_passengers — **PASSED**
> Manifest for IC-701 / 2026-04-01 lists exactly the 4 ticketed passengers
> (P001..P004), rows are sorted ascending by ticket_number, and joined fields
> (class_name, price_paid) are populated by the bulk-fetch enrichment step.

### tests/test_reports/test_train_manifest.py::test_train_manifest_empty_for_unticketed_train — **PASSED**
> Manifest for IC-705 / 2026-04-03 (no tickets in seed) returns 200 with
> total_passengers=0 and rows=[]; header metadata is still populated.

### tests/test_reports/test_train_manifest.py::test_train_manifest_train_not_found — **PASSED**
> Manifest for a non-existent train PK returns 404 TrainNotFound.
